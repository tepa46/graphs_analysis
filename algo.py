class Algo:
    # Extracts data from dataset in the format required for run algorithm
    #
    # NOTE! Use received data as [data] argument in the run method.
    def load_data_from_dataset(self, dataset):
        pass

    # Runs algorithm
    #
    # [additional_data] is additional data needed to run the algorithm
    # For example, start nodes (sources) for parent-BFS algorithms.
    def run(self, data, additional_data=None):
        pass
